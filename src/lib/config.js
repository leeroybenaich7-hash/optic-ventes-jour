// ============================================================
// CONFIGURATION SUPABASE
// Laisser vide = l'app fonctionne en mode local (données dans
// le navigateur du PC). Remplir les deux valeurs = données en
// ligne, partagées et sauvegardées (recommandé).
// Les valeurs se trouvent dans Supabase → Settings → API.
// ============================================================
export const SUPABASE_URL = ''
export const SUPABASE_ANON_KEY = ''

// Version affichée en bas de page — à augmenter à chaque déploiement
// pour forcer le rafraîchissement du cache.
export const APP_VERSION = '10'
